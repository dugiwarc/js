const getMax = (arr) => {
	return Math.max.apply(null, arr);
};

console.log(getMax([1, 3, 5, 7, 9]));

// Language: javascript
// Path: js/toptal/index.js
const getMin = (arr) => {
	return Math.min.apply(null, arr);
};
